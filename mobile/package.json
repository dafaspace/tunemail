{
  "name": "tunemail-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for Tunemail. The web app lives one level up and is copied into www/ by build-www.mjs.",
  "scripts": {
    "build": "node build-www.mjs",
    "sync": "node build-www.mjs && cap sync",
    "ios": "node build-www.mjs && cap sync ios && cap open ios",
    "android": "node build-www.mjs && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1"
  }
}
